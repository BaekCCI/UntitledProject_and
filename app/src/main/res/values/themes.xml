<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UntitledProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>

    </style>

    <style name="Theme.UntitledProject" parent="Base.Theme.UntitledProject" />


    <style name="ThemeOverlay.NumberPicker" parent="Base.Theme.UntitledProject">
        <!-- NumberPicker divider color -->
        <item name="colorControlNormal">@android:color/transparent</item>
        <item name="android:textColorPrimary">@color/gray_700</item>
        <item name="android:textSize">20sp</item>
        <item name="backgroundTint">@color/point_skyblue</item>
    </style>

</resources>